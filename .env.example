# Required for final real evaluation and OpenAI-backed service runs
OPENAI_API_KEY=sk-...
RAG_CONFIG_PATH=config/app.yaml
RAG_LLM_PROVIDER=openai
RAG_MODEL=gpt-5.4-mini
RAG_EVALUATOR_MODEL=gpt-5.4-nano

# Optional local-only smoke test setting; do not use for final submitted metrics
# RAG_LLM_PROVIDER=mock
